export type AaveData = { id: string; positionCount: number; borrowCount: number };
export type AaveDataList = AaveData[];

export type CompoundData = { id: string; positionCount: number };
export type CompoundDataList = CompoundData[];

export type CreamData = { id: string; hasBorrowed: boolean };
export type CreamDataList = CreamData[];

export type EulerData = { id: string };
export type EulerDataList = EulerData[];
