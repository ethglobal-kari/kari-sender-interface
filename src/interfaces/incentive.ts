export interface CreateIncentiveResponseData {
  incentiveId: string;
  campaignId: string;
  audienceSize: number;
  totalAmount: number;
  contractAddress: string;
  tokenAddress: string;
  rootHash: string;
  txhash: string;
  chainId: number;
  description: string;
  deletedAt: string;
  createdAt: string;
  updatedAt: string;
}
